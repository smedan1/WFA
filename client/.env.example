# Server public URL — only needed in production (Railway sets this automatically).
# Omit for local dev: Vite proxy forwards /api → http://localhost:3001
VITE_API_URL=https://wfaserver-production.up.railway.app
